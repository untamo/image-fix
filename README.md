# LineLift

LineLift is a browser-only image editor for detecting and removing distracting vertical lines.

## Use it

Open `index.html` in a modern browser, drop in an image, review the highlighted guides, and choose **Remove detected lines**. The cleaned image can be downloaded as a PNG.

The editor processes the image locally in the browser. No upload or server is required.
